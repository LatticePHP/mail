{
    "name": "lattice/mail",
    "description": "Mail sending and transport abstraction for the LatticePHP framework",
    "type": "library",
    "license": "MIT",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "repositories": [
        {
            "type": "path",
            "url": "../contracts"
        }
    ],
    "require": {
        "php": "^8.4",
        "lattice/contracts": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "Lattice\\Mail\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Lattice\\Mail\\Tests\\": "tests/"
        }
    }
}
